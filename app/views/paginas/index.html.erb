<div class="page-header">
    <h1>Lista de Paginas</h1>
</div>

<div class="alert alert-block">
  <button type="button" class="close" data-dismiss="alert">×</button>
  <h4>Recomendaciones</h4>
  <ul>
    <li>No se pueden Agregar ni eliminar paginas por que forman parte del menu principal del CIIS Tacna 2012, Lo que si se puede es editar las paginas.</li>
    <li>El editor de contenido de las paginas acepta (control + c) para copiar imagenes de cualquier sitio web, se recomienda de images de google.com</li>
  </ul>
</div>

<table class="table table-striped table-bordered">
  <tr>
    <th>#ID</th>
    <th>Titulo</th>
    <th>Descripcion</th>
    <th>Acciones</th>
  </tr>

<% @paginas.each do |pagina| %>
  <tr>
    <td><%= pagina.id %></td>
    <td><%= pagina.titulo[0,60] %></td>
    <td><%= raw(pagina.descripcion[0,50]) %></td>
    <td>
      <% if can? :show, pagina %>
        <%= link_to 'Ver', pagina, :class => "btn" %>
      <% end %>
      <% if can? :update, pagina %>
        <%= link_to 'Editar', edit_pagina_path(pagina), :class => "btn" %>
      <% end %>
      <% if can? :destroy, pagina %>
        <%= link_to 'Del', pagina, confirm: 'Estas Seguro que deseas Eliminar ?', method: :delete, :class => "btn" %>
      <% end %>
    </td>
  </tr>
<% end %>
</table>

<% if can? :create, Pagina %>
<div class="form-actions">
    <%= link_to 'Nueva Pagina', new_pagina_path, :class=>"btn btn-primary" %>
</div>
<% end %>
