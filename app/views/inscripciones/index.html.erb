<section>
  <div class="page-header">
    <h1>Inscritos al CIIS Tacna 2012</h1>
  </div>
  <div class="alert alert-block">
    <button type="button" class="close" data-dismiss="alert">×</button>
    <h4>Recomendaciones</h4>
    <p>Este panel se podria usar para llevar las cuentas online de los inscritos pero se recomienda tener siempre una copia en papel.</p>
  </div>
  <table class="table table-striped table-bordered">
    <tr>
      <th>#id</th>
      <th>Nombre</th>
      <th>Correo</th>
      <th>Institucion</th>
      <th></th>
      <th>Acciones</th>
    </tr>

    <% @inscripciones.each do |inscripcione| %>
        <tr>
          <td><%= inscripcione.id %></td>
          <td><%= inscripcione.nombre[0,30] %></td>
          <td><%= inscripcione.correo[0,30] %></td>
          <td><%= inscripcione.institucion[0,10] %></td>
          <td><%= inscripcione.departamento %></td>
          <td>
            <% if can? :show, inscripcione %>
                <%= link_to 'Ver', inscripcione, :class => "btn" %>
            <% end %>
            <% if can? :update, inscripcione %>
                <%= link_to 'Edit', edit_inscripcione_path(inscripcione), :class => "btn" %>
            <% end %>
            <% if can? :destroy, inscripcione %>
                <%= link_to 'Del', inscripcione, confirm: 'Are you sure?', method: :delete, :class => "btn" %>
            <% end %>
          </td>
        </tr>
    <% end %>
  </table>
</section>

<% if can? :create, @inscripciones %>
<div class="form-actions">
  <%= link_to raw('<i class="icon-lock icon-white"></i> Nueva Inscripcion'), new_inscripcione_path, :class => "btn btn-primary" %>
</div>
<% end %>

